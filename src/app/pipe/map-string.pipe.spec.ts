import { MapStringPipe } from './map-string.pipe';

describe('MapStringPipe', () => {
  it('create an instance', () => {
    const pipe = new MapStringPipe();
    expect(pipe).toBeTruthy();
  });
});
